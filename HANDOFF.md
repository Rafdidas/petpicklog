# HANDOFF.md — AI 간 인수인계

다른 AI 세션이 이어받을 때 가장 먼저 읽는 파일이다. **"지금 어디까지 왔고, 다음에 뭘 하면 되는가"**를 담는다.
영구 규칙·아키텍처는 [AGENTS.md](AGENTS.md)와 [설계서](docs/superpowers/specs/2026-06-29-petfit-redesign-design.md)에 있다. 이 파일은 **휘발성 진행 상태**만 다룬다.

---

## 업데이트 규칙 (필수)

1. **작업을 끝내거나 세션을 넘기기 전에 반드시 이 파일을 갱신**한다.
2. 맨 위 `## 현재 상태`를 항상 최신으로 덮어쓴다. 과거 이력은 `## 변경 로그`에 한 줄로 누적한다.
3. 날짜는 절대값으로 적는다(예: 2026-06-29). "오늘/어제" 금지.
4. 영구적 사실(컨벤션·구조·결정의 근거)은 여기 두지 말고 `AGENTS.md`나 설계서로 옮긴다. 이 파일은 짧게 유지한다.
5. 확정되지 않은 결정은 `## 열린 질문`에 남기고, 임의로 구현하지 않는다.
6. 커밋 전 상태와 실제 파일 상태가 어긋나면 사실대로 적는다(거짓 "완료" 금지).

---

## 현재 상태 (2026-06-29)

**단계: 설계 확정 완료, 구현 시작 전.**

- 펫핏 전면 재설계의 컨셉·IA·데이터·가격추적·범위까지 합의 완료.
- 설계서 작성됨: `docs/superpowers/specs/2026-06-29-petfit-redesign-design.md` (단일 소스).
- 이전 설계서 5개(`pet_life_platform_*`, `pet_price_info_service_*`) 삭제됨.
- `AGENTS.md`, `HANDOFF.md` 생성.
- **아직 코드 구현 0%.** 기존 `src/*`는 손대지 않음.
- **아직 git 커밋 안 함.** 브랜치 `main`. 변경분은 워킹트리에만 존재.

### 다음 할 일 (순서)

1. 작업 브랜치 생성 후 현재 변경(설계서/AGENTS/HANDOFF, 이전 설계서 삭제) 커밋.
2. `writing-plans` 스킬로 구현 계획 문서 작성.
3. 계획에 따라 구현 시작 — 설계서 13장 우선순위 참고:
   디자인 토큰/레이아웃 → `pets`/`/pets` → `lib/curation`+홈 → `/products` → 상세+그래프 → `/saved` → cron 갱신.

### 손대면 안 되는 것 / 주의

- 프론트 UI는 전면 신규(기존 화면 폐기). 단, `lib/naver-shopping`·`lib/pet-search`·`api/products/search`·Supabase 스키마·auth는 재사용.
- `animal_hospitals`, `/hospitals`, `/guide`는 이번 범위 밖(삭제 금지, 보류).
- 서버 시크릿(네이버 키, cron 토큰) 클라이언트 노출 금지.

---

## 열린 질문

- (없음) — 설계 합의 완료. 새로 생기면 여기 기록.

---

## 변경 로그

- 2026-06-29 — 펫핏 재설계 브레인스토밍 완료, 설계서 작성, 이전 설계서 5개 삭제, AGENTS.md·HANDOFF.md 생성. 커밋/구현 전.
